[Unit]
Description={{ ya_requestor_name }} ya-requestor yagna
After=network.target

[Service]
Type=notify
WorkingDirectory={{ ya_requestor_dir }}
ExecStart={{ ya_requestor_dir }}/run_yagna.sh
TimeoutStartSec=10m
LimitNOFILE=10000
Restart=always

[Install]
WantedBy=default.target
